# assets/

This folder is reserved for future static assets such as:
- Custom icons (`.svg`, `.png`)
- Background images
- Fonts (if self-hosting instead of Google Fonts CDN)

Currently empty — the app uses emoji icons and CDN fonts.
